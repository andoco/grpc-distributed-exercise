## Build

```
npm install
npm build
```

## Run

Stateless variant:

```
npm start -- --port=8888 --maxReceive=100 stateless
```

Stateful variant:

```
npm start -- --port=8888 --maxReceive=100 stateful
```